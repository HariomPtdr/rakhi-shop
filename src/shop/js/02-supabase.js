/* ══════════════════════════════════════════════════════════
   SUPABASE — accounts, saved baskets, wishlists, orders, photos

   There is nothing to fill in here. The client in
   src/shared/js/02-sb.js has already configured itself from
   the environment (src/shared/js/00-env.js, written by
   build.py from .env or from Netlify). docs/DEPLOY.md is the
   step by step.

   With the environment empty, SB.on is false and the page is
   exactly what it was before there was a database: no account
   button, the basket on the phone, the catalogue below, and
   not one network call.
   ══════════════════════════════════════════════════════════ */
